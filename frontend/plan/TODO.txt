Completed:
[x] put the new live view to frontend page 3 (live view)
    first put the UI only(dont know about the backend logic because you will have to have the backend and ai agent know what happening, if too complicated leave it)

Pending:
[] if an ai is already at the judge table then go to a diff spot pull the backend too

