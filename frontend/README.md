# 🚀 Project Name (Frontend)

## Overview
This is the frontend application for **[Project Name]**, built with a heavy focus on performance, responsive design, and premium 3D web experiences. It leverages Next.js for routing and server-side rendering, intertwined with React Three Fiber for WebGL/3D graphics, and smooth scroll animations.

## Tech Stack & Skills
Our frontend ecosystem relies on the following core technologies:

* **Framework:** React + Next.js (TypeScript, App Router)
* **3D & WebGL:** `three`, `@react-three/fiber`, `@react-three/drei`, `three-stdlib`
* **Styling & UI:** Tailwind CSS, `shadcn/ui`, Radix UI primitives
* **Animation & Camera:** `motion`, `lenis` (smooth scroll), `@shadergradient/react`, `camera-controls`
* **Agent Skills:** Configured for Antigravity IDE (Taste-skill, R3F-skills, Hallmark, etc.)

## Local Development

1. **Install dependencies:**
   Make sure you are in the correct directory, then run:
   ```bash
   npm install