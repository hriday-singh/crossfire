# Crossfire Frontend Configuration
# By default, Vite proxies /cases requests to http://localhost:8000 in dev and preview modes.
# If connecting directly to the backend without the dev proxy, set:
# VITE_API_BASE_URL=http://localhost:8000
